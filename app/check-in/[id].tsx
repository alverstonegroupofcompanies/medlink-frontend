// Base file for Expo Router - re-exports from platform-specific files
// This file serves as a fallback when platform-specific files are used

// For native platforms (iOS/Android), use the native implementation
export { default } from './[id].native';
